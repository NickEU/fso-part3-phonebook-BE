{
  "name": "fullstackopen-part3",
  "version": "1.0.0",
  "description": "FSO part 3 HW solutions",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../fso-part3-phonebook-FE && npm run build --prod && cp -r build ../fso-part3-phonebook-BE/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && echo 'deploying stuff: ' && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
